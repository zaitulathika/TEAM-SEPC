const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.main-nav');toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');toggle?.setAttribute('aria-expanded','false');}));document.addEventListener('click',e=>{if(!nav?.classList.contains('open'))return;if(nav.contains(e.target)||toggle?.contains(e.target))return;nav.classList.remove('open');toggle?.setAttribute('aria-expanded','false');});const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());

const departmentVideo=document.getElementById("departmentVideo"),soundToggle=document.getElementById("soundToggle");if(departmentVideo&&soundToggle){soundToggle.addEventListener("click",async()=>{departmentVideo.muted=!departmentVideo.muted;if(!departmentVideo.muted){try{await departmentVideo.play()}catch(e){}}soundToggle.innerHTML=departmentVideo.muted?'🔇 <span>Enable Sound</span>':'🔊 <span>Sound On</span>'})}
document.querySelectorAll(".nav-dropdown").forEach(d=>{const b=d.querySelector(".dropdown-toggle");b&&b.addEventListener("click",e=>{e.stopPropagation();const o=!d.classList.contains("open");document.querySelectorAll(".nav-dropdown.open").forEach(x=>x.classList.remove("open"));d.classList.toggle("open",o);b.setAttribute("aria-expanded",String(o))})});document.addEventListener("click",()=>document.querySelectorAll(".nav-dropdown.open").forEach(d=>d.classList.remove("open")));

// Team cards: hover on desktop, tap on phone/tablet.
document.querySelectorAll('.team-card').forEach(card=>{card.addEventListener('click',()=>{if(window.matchMedia('(hover: none)').matches||window.innerWidth<=800)card.classList.toggle('is-flipped')});card.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();card.classList.toggle('is-flipped')}})});
